{
  "name": "@jonrenzo/shared",
  "version": "0.1.0",
  "description": "Shared domain types, state model, and API contract for the TVPH copper traceability concept.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
